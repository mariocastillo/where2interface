<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/favoritos"
            android:icon="@drawable/ic_favorite_black_24dp"
            android:title="Favoritos" />
        <item
            android:id="@+id/verdespues"
            android:icon="@drawable/ic_watch_later_black_24dp"
            android:title="Ver luego" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/compartidos"
                android:icon="@drawable/ic_menu_share"
                android:title="Compartidos" />

            <item
                android:id="@+id/recomendados"
                android:icon="@drawable/ic_record_voice_over_black_24dp"
                android:title="Recomendados" />
            <item
                android:id="@+id/comentarios"
                android:icon="@drawable/ic_comment_black_24dp"
                android:title="Comentarios" />
            <!--<item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send" />-->
        </menu>
    </item>

</menu>
